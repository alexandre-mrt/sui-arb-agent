{
	"name": "sui-arb-keeper",
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"dev": "bun run src/index.ts",
		"build": "bun build src/index.ts --outdir dist --target bun",
		"test": "vitest run",
		"test:watch": "vitest",
		"test:coverage": "vitest run --coverage",
		"lint": "bunx biome check --write .",
		"check": "bunx biome check ."
	},
	"dependencies": {
		"@mysten/sui": "^1.21.0",
		"@mysten/deepbook": "^0.9.0",
		"@cetusprotocol/cetus-sui-clmm-sdk": "^5.0.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"@types/bun": "^1.2.0",
		"vitest": "^3.0.0",
		"typescript": "^5.7.0"
	}
}
